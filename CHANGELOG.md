# 更新日志

本文件记录 TrafficMonitorMedia 的主要版本变更。

## [1.0.3] - 2026-07-29

### 新增

- 新增媒体卡片，可集中查看当前媒体信息、播放进度和可用控制；支持点击卡片外部区域或按 `Esc` 关闭，外部点击仍直接作用于实际目标，并兼容自动隐藏任务栏。
- 媒体卡片支持切换曲目、播放/暂停、切换媒体会话，以及点击或拖动进度条调整播放位置。
- 新增“打开媒体卡片”鼠标动作，可分配给左键单击、左键双击或右键单击。

### 改进

- 重新设计插件选项窗口：支持调整大小、分组显示、紧凑双列布局和内容滚动。
- 新增标题最小宽度设置，并自动保持与标题最大宽度的有效范围关系。
- 系统音量步长默认值调整为 4%。
- 默认鼠标绑定调整为：左键单击打开媒体卡片、左键双击播放/暂停、滚轮切换媒体会话、右键无操作。
- 优化左键单击与双击的识别：双击优先执行，避免同时触发单击动作。
- 媒体卡片从下方向上显示并向下淡出关闭。

## [1.0.2] - 2026-07-28

### 新增

- 新增可配置鼠标滚轮动作：无操作、上一首/下一首、上一/下一媒体会话、增减系统音量。
- 新增系统主音量增减能力，支持在插件选项中设置音量步长，默认 5%，范围 1–50%。

### 改进

- 播放进度条改为叠绘，不再为进度条预留高度，减少对标题和艺术家文本空间的挤占。
- 修复插件选项中“标题最大宽度”的单位文本换行问题。
- 构建和发布目标更新为 x86/x64/ARM64EC，并保持 VS 工程内部 Win32 平台映射。
- 更新插件版本为 1.0.2。

## [1.0.1] - 2026-07-27

### 新增

- 新增可选的媒体封面背景，从 Windows GSMTC 读取当前媒体缩略图。
- 新增媒体封面平滑缩放选项，默认开启。
- 使用 Material Design Icons 的 `motion-play` 图标作为插件图标。

### 改进

- 媒体封面保持原始比例并居中裁剪，使用深色遮罩保证标题、状态图标和进度条可读。
- 平滑缩放使用高质量双三次插值；高质量绘制不可用或关闭时回退到 HALFTONE 缩放。
- 切歌或切换媒体会话时同步更新并缓存封面，缺少或无法解码封面时自动回退到原任务栏背景。

## [1.0.0] - 2026-07-27

### 新增

- 显示当前媒体标题，并可在第二行显示艺术家。
- 显示播放、暂停或无媒体状态图标。
- 显示当前媒体播放进度。
- 支持播放/暂停、上一首和下一首媒体控制。
- 支持分别配置左键单击、左键双击和右键单击动作。
- 支持通过鼠标滚轮循环切换多个媒体会话。
- 支持中英文插件选项和配置持久化。
- 新增 Win32/x64 持续集成和版本标签自动发布工作流。
- 统一本地、CI 和发布流程的构建及 DLL 导出验证入口。
