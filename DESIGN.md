# TrafficMonitorMedia 功能设计

## 1. 项目定位

TrafficMonitorMedia 是一个用于 [TrafficMonitor](https://github.com/zhongyang219/TrafficMonitor) 的 Windows 媒体信息插件。

插件在 TrafficMonitor 任务栏窗口中显示当前媒体标题、艺术家、播放状态和进度，并提供常用媒体控制及多媒体会话切换功能。

当前版本：`1.0.0`。

## 2. 媒体显示

插件提供一个显示项：`媒体标题`。

### 2.1 标题与艺术家

正常状态下，第一行显示媒体标题。

“第二行显示艺术家”默认开启：

- 播放器提供艺术家时，第二行显示艺术家。
- 播放器未提供艺术家时，标题使用可用文本区域，最多绘制两行。
- 关闭该选项后，标题直接使用完整文本区域，最多绘制两行。

标题为空但来源应用标识可用时，使用来源应用标识代替标题。长文本根据标题最大宽度和实际显示区域换行或省略。

### 2.2 状态图标

标题左侧默认显示媒体状态对应的操作图标：

- 正在播放：显示暂停图标。
- 已暂停：显示播放图标。
- 无媒体、加载中或状态未知：显示无媒体图标。

图标分别适配深色和浅色任务栏。“显示状态图标”关闭后，不绘制图标，也不保留图标和间距占用的宽度。

### 2.3 播放进度

当播放器提供有效播放时间线时，标题底部显示播放进度条：

- 底色表示完整时长。
- 高亮部分表示当前播放位置。
- 没有有效时间线时不显示进度条。
- “显示播放进度条”关闭后始终不显示进度条。

### 2.4 状态文本

媒体不可用时使用以下文本：

| 状态 | 显示文本 |
|---|---|
| 正在初始化 | 正在获取媒体… |
| 没有媒体会话 | 未检测到媒体 |
| 媒体服务异常 | 媒体不可用 |

### 2.5 悬停提示

Tooltip 保持为纯文本：

- 第一行显示标题，标题不可用时显示来源应用标识或状态文本。
- 艺术家可用时在第二行显示艺术家。
- 媒体服务异常时附加错误信息。

Tooltip 不添加“当前媒体：”前缀，不显示状态图标，也不显示媒体会话序号。

## 3. 媒体控制

### 3.1 可配置鼠标动作

左键单击、左键双击和右键单击可以分别设置为：

- 无操作。
- 播放/暂停。
- 上一首。
- 下一首。

默认映射：

| 触发方式 | 默认动作 |
|---|---|
| 左键单击 | 播放/暂停 |
| 左键双击 | 下一首 |
| 右键单击 | 无操作 |

双击动作优先，不会额外触发等待中的单击动作。右键设置为“无操作”时，插件不消费右键事件，保留 TrafficMonitor 的默认菜单。

### 3.2 多媒体会话切换

系统中同时存在多个媒体会话时，在标题区域使用滚轮切换：

- 滚轮向上：上一媒体会话。
- 滚轮向下：下一媒体会话。
- 到达首尾时循环切换。

切换后，标题、艺术家、状态、进度、Tooltip 和媒体控制目标同步使用所选会话。

无媒体或只有一个媒体会话时，插件不消费滚轮事件。任务栏中不额外显示会话序号、按钮或方向图标。

## 4. 插件选项

| 设置项 | 说明 | 默认值 |
|---|---|---|
| 显示播放进度条 | 有有效时间线时显示标题底部进度 | 开启 |
| 显示状态图标 | 显示播放、暂停或无媒体图标 | 开启 |
| 第二行显示艺术家 | 艺术家可用时在第二行显示 | 开启 |
| 标题最大宽度 | 限制显示项文本宽度，范围为 100–1000 逻辑像素 | 400 |
| 左键单击 | 配置单击动作 | 播放/暂停 |
| 左键双击 | 配置双击动作 | 下一首 |
| 右键单击 | 配置右键动作 | 无操作 |

滚轮固定用于切换媒体会话，不提供动作映射选项。

设置由插件自动持久化。配置缺失或单项值无效时使用默认值；标题宽度会限制到有效范围，未知动作会回退到默认映射。

## 5. 兼容与降级

插件使用 Windows Global System Media Transport Controls（GSMTC）获取媒体信息和发送控制命令。

运行要求：

- Windows 10 1809 或更高版本。
- 支持插件功能的 TrafficMonitor。
- 插件 DLL 位数与 TrafficMonitor 位数一致。
- 播放器创建 GSMTC 媒体会话。

不同播放器提供的数据和控制能力可能不同：

- 没有艺术家时只显示标题。
- 没有播放时间线时隐藏进度条。
- 不支持某项媒体控制时，该操作可能无效果。
- 当前媒体会话消失时，自动回退到仍然可用的会话；没有会话时进入无媒体状态。

单项媒体属性缺失不会影响插件继续运行。

## 6. 已知限制

### 6.1 双行独占布局

插件会请求 TrafficMonitor 分配独占双行区域，但旧版宿主可能忽略该请求。TrafficMonitor V1.86 中可以绘制双行内容，但插件无法保证显示项独占完整一列。

### 6.2 任务栏绝对位置

TrafficMonitor 插件接口不提供 Windows 任务栏对齐状态，也不允许插件重排显示项或移动 TrafficMonitor 任务栏窗口，因此当前无法实现：

- Windows 任务栏图标居中时，将插件整体放到任务栏左侧。
- Windows 任务栏图标靠左时，将插件整体放到任务栏中央。

### 6.3 歌词

GSMTC 没有标准歌词字段，当前已测试的媒体会话也未提供可用歌词。版本 `1.0.0` 不支持歌词显示。

## 7. 未来功能

### 7.1 歌词显示

在明确可靠数据源后重新设计歌词功能，可评估本地 LRC、播放器公开接口或用户明确选择的联网歌词服务。

歌词应作为独立显示项提供，允许用户在 TrafficMonitor 中单独启用、排序和隐藏。

### 7.2 任务栏布局控制

如果未来 TrafficMonitor 提供正式的任务栏对齐查询、显示项布局或窗口定位接口，再实现根据 Windows 任务栏对齐方式切换插件整体位置。

### 7.3 双行独占兼容

当目标 TrafficMonitor 版本普遍支持独占双行布局后，重新验证标题项能否稳定占据完整一列。
