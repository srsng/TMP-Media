# TrafficMonitorMedia 功能设计

## 1. 项目定位

TrafficMonitorMedia 是一个用于 [TrafficMonitor](https://github.com/zhongyang219/TrafficMonitor) 的 Windows 媒体信息插件。

插件在 TrafficMonitor 任务栏窗口中显示当前媒体标题、艺术家、播放状态、播放进度和可选的媒体封面背景，并提供常用媒体控制、多媒体会话切换和系统主音量调整功能。

当前版本：`1.0.3`。

## 2. 媒体显示

插件提供一个显示项：`媒体标题`。

### 2.1 标题与艺术家

正常状态下，第一行显示媒体标题。

“第二行显示艺术家”默认开启：

- 播放器提供艺术家时，第二行显示艺术家。
- 播放器未提供艺术家时，标题使用可用文本区域，最多绘制两行。
- 关闭该选项后，标题直接使用完整文本区域，最多绘制两行。

标题为空但来源应用标识可用时，使用来源应用标识代替标题。长文本根据标题最小宽度、标题最大宽度和实际显示区域换行或省略。

### 2.2 状态图标

标题左侧默认显示媒体状态对应的操作图标：

- 正在播放：显示暂停图标。
- 已暂停：显示播放图标。
- 无媒体、加载中或状态未知：显示无媒体图标。

图标分别适配深色和浅色任务栏。“显示状态图标”关闭后，不绘制图标，也不保留图标和间距占用的宽度。

### 2.3 播放进度

当播放器提供有效播放时间线时，标题底部显示播放进度条：

- 底色表示完整时长。
- 高亮部分表示当前播放位置。
- 没有有效时间线时不显示进度条。
- “显示播放进度条”关闭后始终不显示进度条。
- 进度条使用叠绘，不再为自身预留垂直高度，避免挤占标题和艺术家文本空间。

绘制层级为：任务栏原背景或封面背景 → 进度条 → 状态图标与文字。

### 2.4 媒体封面背景

“显示媒体封面背景”默认关闭。开启后，如果当前播放器通过 Windows GSMTC 提供媒体缩略图，插件会将封面作为媒体标题显示项的背景：

- 封面保持原始比例并居中裁剪，填满插件实际获得的显示区域。
- “平滑缩放媒体封面”默认开启，用于改善封面缩放后的显示效果；关闭后使用系统缩放路径。
- 高质量绘制不可用时自动回退，不会导致封面消失。
- 封面上覆盖深色半透明遮罩，文字和状态图标使用适合深色背景的样式。
- 当前媒体没有封面，或封面读取、解码失败时，自动回退到原有任务栏背景，不影响其他媒体信息和控制功能。
- 切歌或切换媒体会话时，封面与标题、艺术家和播放状态同步更新。

封面只覆盖 TrafficMonitor 分配给该显示项的矩形。插件不能越过宿主边界绘制，也不能借此强制占据完整一列。

### 2.5 状态文本

媒体不可用时使用以下文本：

| 状态 | 显示文本 |
|---|---|
| 正在初始化 | 正在获取媒体… |
| 没有媒体会话 | 未检测到媒体 |
| 媒体服务异常 | 媒体不可用 |

### 2.6 悬停提示

Tooltip 保持为纯文本：

- 第一行显示标题，标题不可用时显示来源应用标识或状态文本。
- 艺术家可用时在第二行显示艺术家。
- 媒体服务异常时附加错误信息。

Tooltip 不添加“当前媒体：”前缀，不显示状态图标、封面或媒体会话序号。

### 2.7 媒体卡片

媒体卡片用于在不扩展任务栏显示项的前提下集中展示和控制当前媒体：

- 显示媒体来源标识、封面、标题、艺术家、当前时间、总时长和播放进度。
- 提供上一首、播放/暂停、下一首控制。
- 提供上一/下一媒体会话切换；只有一个会话时不执行无意义切换。
- 支持点击进度条跳转播放位置，也支持拖动预览并在松开时提交一次跳转。
- 播放器未提供封面、有效时间线或某项控制能力时，对应内容自动降级或禁用。

卡片保持单实例，显示在触发任务栏位置附近，并限制在该显示器工作区内。它是唯一新增的顶层窗口，只占据卡片自身矩形；从主任务栏或副任务栏打开时，仅对白名单中的任务栏根窗口建立 owner 关系。卡片参与正常激活生命周期，点击卡片外区域或按 `Esc` 都会进入统一关闭流程；外部点击直接交给实际目标，插件不覆盖桌面、任务栏或屏幕边缘。

## 3. 控制能力

### 3.1 可配置鼠标动作

左键单击、左键双击和右键单击可以分别设置为：

- 无操作。
- 打开媒体卡片。
- 播放/暂停。
- 上一首。
- 下一首。

默认映射：

| 触发方式 | 默认动作 |
|---|---|
| 左键单击 | 打开媒体卡片 |
| 左键双击 | 播放/暂停 |
| 右键单击 | 无操作 |

双击动作优先，不会额外触发等待中的单击动作。右键设置为“无操作”时，插件不消费右键事件，保留 TrafficMonitor 的默认菜单。

### 3.2 可配置滚轮动作

滚轮动作按“向上/向下”成对配置：

| 滚轮动作 | 向上 | 向下 | 默认 |
|---|---|---|---|
| 无操作 | 不处理 | 不处理 | 否 |
| 上一首/下一首 | 上一首 | 下一首 | 否 |
| 上一/下一媒体会话 | 上一媒体会话 | 下一媒体会话 | 是 |
| 增减系统音量 | 增大系统音量 | 减小系统音量 | 否 |

默认行为保持为滚轮切换媒体会话。媒体会话切换到达首尾时循环；无媒体或只有一个媒体会话时，插件不消费该滚轮事件。任务栏中不额外显示会话序号、按钮或方向图标。

### 3.3 系统主音量调整

当滚轮动作设置为“增减系统音量”时，插件调整 Windows 当前默认播放设备的系统主音量：

- 向上滚动按配置步长增大音量。
- 向下滚动按配置步长减小音量。
- 音量范围限制为 0–100%。
- 步长默认 4%，可配置范围为 1–50%。
- 设置音量不自动取消系统静音。

系统音量控制使用 Windows Core Audio EndpointVolume API，作用于当前默认渲染端点。它独立于 GSMTC 媒体服务；系统音量操作失败不影响媒体标题显示、播放控制或媒体会话切换。

## 4. 插件选项

### 4.1 窗口布局

选项窗口支持调整大小并设置最小尺寸。显示设置和鼠标操作使用独立分组；每个分组中的字段按两列紧凑排布。内容区域随窗口宽度伸缩；高度不足时显示垂直滚动条，确定和取消按钮固定在右下角，不随内容滚动。

最小尺寸下各项保持紧凑但可交互；增大窗口时，各分组和输入控件按既定相对位置扩展。“标题最小宽度”和“标题最大宽度”的数值输入均与“逻辑像素”单位始终保持同行。

### 4.2 设置项

| 设置项 | 说明 | 默认值 |
|---|---|---|
| 显示播放进度条 | 有有效时间线时以叠绘方式显示标题底部进度 | 开启 |
| 显示状态图标 | 显示播放、暂停或无媒体图标 | 开启 |
| 第二行显示艺术家 | 艺术家可用时在第二行显示 | 开启 |
| 显示媒体封面背景 | 播放器提供封面时将其绘制为显示项背景 | 关闭 |
| 平滑缩放媒体封面 | 改善封面缩放后的显示效果，仅在封面背景开启时生效 | 开启 |
| 标题最小宽度 | 设置显示项文本的最小宽度，范围为 100–1000 逻辑像素；不会超过标题最大宽度 | 100 |
| 标题最大宽度 | 限制显示项文本宽度，范围为 100–1000 逻辑像素；不会小于标题最小宽度 | 400 |
| 左键单击 | 配置单击动作 | 打开媒体卡片 |
| 左键双击 | 配置双击动作 | 播放/暂停 |
| 右键单击 | 配置右键动作 | 无操作 |
| 滚轮 | 配置滚轮动作 | 上一/下一媒体会话 |
| 音量步长 | 配置滚轮增减系统音量的百分比步长，范围为 1–50 | 4 |

设置由插件自动持久化。配置缺失或单项值无效时使用默认值；标题宽度和音量步长会限制到有效范围，未知动作会回退到默认映射。

## 5. 运行模型与交互边界

GSMTC 媒体工作线程是任务栏显示项和媒体卡片的唯一媒体数据源。工作线程统一获取媒体属性、时间线、封面、会话列表和控制能力，再发布不可变快照供界面读取；任务栏绘制和媒体卡片不直接调用播放器。

播放控制、媒体会话切换和进度跳转由界面提交为异步请求，并在媒体工作线程中执行。媒体卡片可见时会请求封面数据；关闭后不再因为卡片单独加载封面。卡片之外不创建输入截获窗口，外部点击由 Windows 正常路由到用户实际点击的目标，并通过卡片失活触发关闭。

左键单击动作若配置为打开媒体卡片，需要与左键双击动作消歧：左键双击动作为“无操作”时，卡片立即打开；存在左键双击动作时，确认等待取 Windows 系统双击时间与 300ms 的较小值。确认窗口内收到双击会取消待执行的单击，因此正常速度的“双击播放/暂停”不会先打开媒体卡片；当用户设置的系统双击时间大于 300ms 时，慢于 300ms 的双击允许先打开卡片再执行双击动作。

媒体卡片创建后先在隐藏状态应用入场初始帧，再正常显示并请求一次前台激活；实际收到激活消息后，才将其视为进入了可靠的弹出生命周期。若首轮异步验证仍未激活，只允许进行一次补偿请求；再次验证失败时直接收敛到关闭状态，不循环抢占前台。

卡片失活时只投递延迟关闭请求，由管理器再次确认卡片实例、生命周期状态和当前前台窗口后进入出场流程，避免在窗口激活切换的同步调用栈内销毁窗口。重新激活、旧窗口消息或已进入关闭状态的请求都会被忽略。卡片使用约 150ms 的自下而上淡入和轻微位移动画进入，关闭时使用约 110ms 的向下淡出和轻微位移动画退出；动画帧仅调整窗口透明度和位置，不重新激活窗口。退出动画结束后统一销毁卡片。

## 6. 兼容与降级

插件使用 Windows Global System Media Transport Controls（GSMTC）获取媒体信息、媒体封面和发送媒体控制命令；使用 Windows Core Audio EndpointVolume API 调整系统主音量。

运行要求：

- Windows 10 1809 或更高版本。
- 支持插件功能的 TrafficMonitor。
- 插件 DLL 位数与 TrafficMonitor 位数一致。
- 播放器创建 GSMTC 媒体会话。

不同播放器提供的数据和控制能力可能不同：

- 没有艺术家时只显示标题。
- 没有播放时间线时隐藏进度条。
- 没有媒体缩略图时不显示封面背景。
- 不支持某项媒体控制时，该操作可能无效果。
- 当前媒体会话消失时，自动回退到仍然可用的会话；没有会话时进入无媒体状态。

单项媒体属性或封面缺失不会影响插件继续运行。封面读取和解码在媒体工作线程中完成，任务栏绘制只使用已经准备好的缓存图像。系统音量服务在独立工作线程中处理请求，不在 TrafficMonitor UI 回调中执行 Core Audio 操作。

## 7. 已知限制

### 7.1 双行独占布局

插件会请求 TrafficMonitor 分配独占双行区域，但旧版宿主可能忽略该请求。TrafficMonitor V1.86 中可能仍只分配半高区域；插件只能在实际获得的矩形内绘制文本和封面，无法强制占据完整一列。

### 7.2 任务栏绝对位置

TrafficMonitor 插件接口不提供任务栏对齐方式、宿主窗口绝对定位或项目重排能力。插件无法根据 Windows 任务栏居中或靠左设置，将显示项移动到任务栏左侧或中央。

### 7.3 媒体数据差异

GSMTC 数据由播放器提供。标题、艺术家、时间线、封面和控制能力均可能缺失或不准确。插件只能对可用数据进行显示和降级。

### 7.4 系统音量边界

系统音量控制作用于 Windows 当前默认播放设备。它不保证影响非默认设备，也不保证影响绕过系统端点音量的独占模式音频流。

### 7.5 歌词

GSMTC 没有标准歌词字段。当前版本不实现播放器私有协议、网页抓取或本地歌词匹配，因此不支持歌词显示。

## 8. 未来功能

### 8.1 歌词显示

如果后续确定稳定且可维护的歌词来源，可新增独立歌词显示项，并提供歌词来源、时间同步和隐私策略。该功能不依赖当前 GSMTC 标准字段。

### 8.2 任务栏布局控制

如果未来 TrafficMonitor 提供正式的任务栏对齐查询、显示项布局或窗口定位接口，再实现根据 Windows 任务栏对齐方式切换插件整体位置。

### 8.3 双行独占兼容

如果未来宿主版本完整支持 `IsDoubleLineExclusive()`，媒体标题及其封面背景可自然使用完整双行区域；插件仍会兼容只分配单行区域的旧版宿主。

### 8.4 封面显示扩展

后续可在不影响默认简洁配置的前提下评估遮罩强度、背景模糊等视觉选项。当前版本固定使用居中裁剪和深色遮罩，不暴露额外参数。

### 8.5 媒体来源信息

当前媒体卡片使用播放器提供的来源应用标识。后续可评估通过 Windows 应用信息接口显示更友好的应用名称和应用图标，并为无法解析的传统桌面应用保留来源标识回退。
